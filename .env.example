# ==================================
# N8N AI WORKFLOW DOCKER COMPOSE ENV
# ==================================
# Copy this file to .env and customize the values below

# ==================================
# PROJECT CONFIGURATION
# ==================================
COMPOSE_PROJECT_NAME=n8n-ai-stack
TIMEZONE=UTC
NETWORK_SUBNET=172.20.0.0/16

# ==================================
# POSTGRESQL DATABASE CONFIGURATION
# ==================================
POSTGRES_VERSION=15-alpine
POSTGRES_DB=n8n
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_postgres_password_here
POSTGRES_NON_ROOT_USER=n8n
POSTGRES_NON_ROOT_PASSWORD=your_secure_n8n_db_password_here
POSTGRES_PORT=5432

# ==================================
# N8N CONFIGURATION
# ==================================
N8N_VERSION=latest
N8N_HOST=localhost
N8N_PORT=5678
N8N_EXTERNAL_PORT=5678
N8N_PROTOCOL=http
N8N_WEBHOOK_URL=http://localhost:5678

# Security (REQUIRED - Generate secure random strings)
N8N_ENCRYPTION_KEY=your_32_character_encryption_key_here
N8N_JWT_SECRET=your_jwt_secret_here

# Database Schema
N8N_DB_SCHEMA=public

# Features & Logging
N8N_METRICS=false
N8N_LOG_LEVEL=info
N8N_LOG_OUTPUT=console
N8N_AI_ENABLED=true
N8N_HIDE_USAGE_PAGE=true

# Execution Configuration
N8N_EXECUTIONS_PROCESS=main
N8N_EXECUTIONS_MODE=regular
N8N_EXECUTIONS_TIMEOUT=3600
N8N_EXECUTIONS_TIMEOUT_MAX=3600

# ==================================
# OLLAMA CONFIGURATION
# ==================================
OLLAMA_VERSION=latest
OLLAMA_EXTERNAL_PORT=11434
OLLAMA_HOST=0.0.0.0
OLLAMA_PORT=11434
OLLAMA_ORIGINS=*

# ==================================
# TRAEFIK CONFIGURATION (OPTIONAL - For Production)
# ==================================
# Uncomment and configure these if using Traefik reverse proxy
# TRAEFIK_VERSION=v2.10
# TRAEFIK_API_INSECURE=false
# DOMAIN=yourdomain.com
# ACME_EMAIL=your-email@yourdomain.com

# ==================================
# SECURITY NOTES
# ==================================
# 1. Generate a secure N8N_ENCRYPTION_KEY:
#    openssl rand -base64 32
#
# 2. Generate a secure N8N_JWT_SECRET:
#    openssl rand -base64 64
#
# 3. Use strong passwords for POSTGRES_PASSWORD and POSTGRES_NON_ROOT_PASSWORD
#
# 4. For production, consider:
#    - Using Docker secrets instead of environment variables
#    - Setting up SSL/TLS certificates
#    - Configuring firewall rules
#    - Regular backups
#
# ==================================
# OLLAMA MODEL MANAGEMENT
# ==================================
# After starting the stack, you can manage Ollama models:
#
# List available models:
# docker exec n8n-ai-stack_ollama ollama list
#
# Pull a model (e.g., llama2):
# docker exec n8n-ai-stack_ollama ollama pull llama2
#
# Pull other useful models for n8n workflows:
# docker exec n8n-ai-stack_ollama ollama pull codellama
# docker exec n8n-ai-stack_ollama ollama pull mistral
# docker exec n8n-ai-stack_ollama ollama pull phi
#
# Check Ollama API:
# curl http://localhost:11434/api/version