# GPU Override Configuration
# Use with: docker-compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.gpu.yml up -d
# This file adds GPU acceleration to the base Ollama service configuration

services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]